# NEXUS OS — Requirements (stdlib only for core)
# Optional — for enhanced features:
# beautifulsoup4>=4.11.0  # Web scraper template (optional)
# requests>=2.28.0        # HTTP calls (optional)

# NEXUS OS core: Python 3.10+ stdlib only — no external dependencies required!
